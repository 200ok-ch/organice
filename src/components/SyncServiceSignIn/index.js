import React, { PureComponent } from 'react';

import './SyncServiceSignIn.css';

export default class SyncServiceSignIn extends PureComponent {
  render() {
    return (
      <div className="sync-service-sign-in-container">
        Sync service sign in
      </div>
    );
  }
}
